<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= (content_for?(:title)) ? yield(:title) + " | " + t(:site_title) : t(:site_title) + " | " + t(:site_tagline) %></title>
    <meta name="description" content="">
    <meta name="author" content="">
    <%= stylesheet_link_tag    "application", "bootstrap", "bootstrap-custom" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon" href="images/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="72x72" href="images/apple-touch-icon-72x72.png">

    <link rel="apple-touch-icon" sizes="114x114" href="images/apple-touch-icon-114x114.png">
  </head>

  <body>

    <div class="topbar">
      <%= render 'layouts/header' %>
    </div>

    <div class="container">

      <div class="content">
        <div class="page-header">
          <h1><%= yield :page_title %> <small><%= yield :page_tagline %></small></h1>
        </div>
        <div class="row">
          <div class="span10">
            <% if content_for :content_title %>
            <h2><%= yield :content_title %></h2>
            <% end %>
            <%= yield %>
          </div>
          <div class="span4">
            <h3><%= t "nav.title" %></h3>
            <ul>
              <li><%= link_to t("nav.home"), root_path %></li>
              <li><%= link_to t("nav.about"), controller: "pages", action: "about" %></li>
              <li><%= link_to t("nav.contact"), controller: "pages", action: "contact" %></li>
            </ul>
          </div>
        </div>
      </div>

      <footer>
        <p>&copy; Company 2011</p>

      </footer>

    </div> <!-- /container -->

  </body>
</html>