- content_for :page_title, t('.title')
- content_for :page_tagline, t('.subtitle')
= stylesheet_link_tag "event_calendar"
= javascript_tag "event_calendar"
- if @event
  .event.featured= render @event
  = @event
%h2 Calendar
%p Below you can view all the events for the upcoming month
= render 'calendar/calendar'
- if can? :create, Event
  .links.well
    = link_to 'New Event', new_event_path, class: 'btn'